const DURATION = `var(--jinni-duration-short4)`;
const TIMING_FUNCTION = `var(--jinni-easing-emphasized)`;
export const TRANSITION = `${DURATION} ${TIMING_FUNCTION}`;

export const DRAWER_WIDTH = 300;
